#include <stdlib.h>
#include <stdio.h>

#include "AIAgent.h"
//HI
AIAgent::AIAgent(unsigned int num_states, int* ls_state, int initState=0, int stateEnd = 0, StateWorker* defaultWorker = NULL):StateMachine(num_states, ls_state, initState, stateEnd, defaultWorker)
{

}



